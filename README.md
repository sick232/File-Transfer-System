# File-Transfer-System
A secure local File Transfer System built using Flask and SQLite — supports file and text sharing, automatic cleanup, and random code-based access, all running safely on localhost.
